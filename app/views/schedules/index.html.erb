<h1>All Schedules</h1>
<% @schedules.each do |schedule| %>
  <h2><%= schedule.plant_id %></h2>
  <p>User id: <%= schedule.user_id %></p>
  <p>Image: <%= schedule.image_url %></p>
  <p>Watering Start Date: <%= schedule.watering_start_date %></p>
<% end %>